# --- Contact form email (Resend) ---
# 1. Create a free account at https://resend.com
# 2. Create an API key and paste it here.
# 3. Copy this file to `.env.local` (which stays out of git) and fill it in.
#    On your host (e.g. AWS Amplify / Vercel), add the same variables in the
#    project's Environment Variables settings.
RESEND_API_KEY=

# Optional — where contact messages are delivered.
# Defaults to denizugur.dev@gmail.com if omitted.
CONTACT_TO_EMAIL=denizugur.dev@gmail.com

# Optional — the "from" address shown on the email.
# Until you verify your own domain in Resend, leave this as the shared
# onboarding sender below. Once your domain is verified, use something like:
#   CONTACT_FROM_EMAIL=Portfolio Contact <hello@deniz-ugur.dev>
CONTACT_FROM_EMAIL=Portfolio Contact <onboarding@resend.dev>

# --- Likes counter (Turso / libSQL) ---
# Persists the "Did you like my website?" count across deploys. On serverless
# hosts (Vercel, AWS Amplify) the local filesystem is wiped on every deploy, so
# without these the count would reset. Turso is a hosted, SQLite-compatible
# database with a free tier.
#
# Set up (one time):
#   1. Install the CLI: https://docs.turso.tech/cli/installation
#   2. turso auth signup
#   3. turso db create portfolio-likes
#   4. turso db show portfolio-likes --url      # -> TURSO_DATABASE_URL
#   5. turso db tokens create portfolio-likes   # -> TURSO_AUTH_TOKEN
# Then add both values in your host's Environment Variables settings.
#
# Leave these blank locally to use a zero-setup SQLite file at data/likes.db.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
